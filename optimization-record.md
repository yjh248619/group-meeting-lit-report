# 技能演进记录

**日期约定（强制）**：每条标题与「日期说明」里的日期，都必须是**本条写入并保存到磁盘当天**的本机公历日期。落盘前用命令核对，禁止沿用对话上下文、训练数据或上一条记录的日期：

```powershell
Get-Date -Format yyyy-MM-dd
```

跨自然日续改同一条时，把标题日期改为**最终落盘日**。

**元规则（强制）**：凡改动本技能目录内的规则/流程/自检类文档（`SKILL.md`、`references/`、`assets/`、`state/` 的协议部分、`scripts/` 的判定逻辑），**须在同一轮内**于本文件末尾**追加一条记录**（至少含：日期说明 / 需求·触因 / 规则摘要 / 波及文件表）。**禁止只改规则不写记录。**

---

### 2026-09-14：建立两阶段组会文献汇报技能（v1）

#### 日期说明

- 本条落盘日：2026-09-14（本机公历，已用 `Get-Date -Format yyyy-MM-dd` 核对）。

#### 需求·触因

- 使用者需要长期做组会文献汇报：指定文献 + 页码范围 → 逐句翻译、内容解析、图表读法讲解 → 导出 LaTeX/PDF/Word。
- 首次实战（Gelman《Bayesian Data Analysis》3rd ed. 课本 p.476–477）暴露出一批高频问题：从图上读的数靠目测容易错、图表坐标轴的读法没人讲清、作者"后文才解释"的内容容易漏、报告内部前后结论会自相矛盾。
- 该次实战交付后做首轮自检，查出 6 处错误（含 1 处实质错误、1 处前后矛盾）与 11 处解释缺口，说明"靠 AI 自觉审查"不可靠。

#### 规则摘要

- 建立两阶段流程：阶段一（逐句翻译 + 图表讲解 + 两轮审查）默认交付 MD；阶段二（LaTeX→PDF / pandoc→Word）需用户确认后启动。
- 四条铁律：证据回溯、阶段闸门、不静默降级、交付物落地。
- 三档标注：A 原文 / B 推论 / C 按图读数，三者不许混。
- 图表"必讲清单"10 条（回答什么问题 / 横坐标 / 纵坐标 / 图形元素 / 精确读数 / 判读清单 / 本图结论 / 读图局限 / 与其他图的接口 / 易错点）。
- 第一轮审查五类：A 前后逻辑 / B 方法论 / C 证据回溯 / D 解释完整性 / E 数值自检。
- 第二轮审查：上下文回溯 → 待确认项清单 → **清单未归零禁止进入阶段二**。

#### 波及文件

| 文件 | 变更摘要 |
|---|---|
| `SKILL.md` | 新建：四铁律、三档标注、阶段总览、插件自检、环境实测 |
| `references/phase1-translate-review.md` | 新建：阶段一全流程与两轮审查清单 |
| `references/phase2-export.md` | 新建：导出格式询问、编译自愈循环、Word 路线 |
| `references/pdf-evidence-extraction.md` | 新建：矢量坐标反算与三种交叉验证手段 |
| `references/latex-cookbook.md` | 新建：可编译骨架与版式宏 |
| `scripts/extract_pages.py` | 新建：一键取证 |
| `scripts/compile_pdf.py` | 新建：编译 + 报错摘要 |
| `optimization-record.md` | 本条 |

---

### 2026-09-14：记录两个真实编译坑（ctexart×enumitem、hyperref×bookmark）

#### 日期说明

- 本条落盘日：2026-09-14（本机公历）。

#### 需求·触因

- 为验证技能的阶段二工具链，做了一次端到端冒烟测试（3 页中文 PDF，含图片嵌入、longtable、宽表缩放、三档标注宏、公式、问答列表）。第一次编译失败，报错：
  `Argument of \enit@postlabel@i has an extra }`，指向 `\item[Q1 纵轴为什么取对数？]`。
- 做对照实验定位根因（5 + 5 组对照）：
  | 组合 | 结果 |
  |---|---|
  | `article` + `description` + `style=nextline` | **通过** |
  | `ctexart` + `itemize` + `style=nextline` | **通过** |
  | `ctexart` + `description` + `style=nextline` | **失败** |
  | `ctexart` + `description`（不用 nextline） | **通过** |
  | `ctexart` + `description` + `font=\bfseries,leftmargin` | **通过** |
  | `ctexart` + `description` + 标签用花括号包裹 | **失败** |
  | `ctexart` + `description` + 标签后加空格 | **失败** |
  说明这与中文标点无关（拉丁标签同样失败），是 **ctexart × enumitem(v3.11, 2025/02/06) × description × style=nextline** 的组合冲突。

#### 规则摘要

- **禁止**在 `ctexart` 下使用 `\begin{description}[style=nextline, …]`。
- 替代写法（已验证）：`\begin{description}[font=\bfseries,leftmargin=1.5em,itemsep=0.4em]`；若需"问题独占一行"，用自定义宏 `\newcommand{\QA}[2]{…}`。
- **禁止**在 `\hypersetup` 里传 `bookmarks=true`（与 `bookmark` 宏包冲突，产生 `Option 'bookmarks' has already been used` 警告）。
- 编译前先核对图片路径存在——缺图是最常见的编译失败原因，`compile_pdf.py` 会把它报成"缺少文件"并给出出错行。

#### 波及文件

| 文件 | 变更摘要 |
|---|---|
| `references/latex-cookbook.md` | §4 CJK 专属坑表新增两条；§2.5 会话式问答改为已验证写法；§1 骨架的 `\hypersetup` 去掉 `bookmarks=true` |
| `references/phase2-export.md` | 导言区一节改为指向 cookbook，避免两处重复维护 |
| `optimization-record.md` | 本条 |

---

### 2026-09-14：加入静态门禁脚本，把"审查"从自觉变成可验证

#### 日期说明

- 本条落盘日：2026-09-14（本机公历）。

#### 需求·触因

- 用户指出：技能的"两轮审查"完全依赖 AI 自觉，没有任何东西能阻止它**声称**审过了；并提供一个参照工程（某前端的 `.cursor` 目录）——那套体系用 `rules`（三层分级）+ `hooks`（门禁脚本 + git 哈希状态机）+ `skills` 来"把 AI 的自觉变成可验证的机制"。
- 调研结论：可迁移的主线只有"静态脚本的退出码、固定语留痕、模板固化、证据纪律"；`hooks.json`、git 哈希状态机、`globs` 触发、MySQL 技能主库等因本环境无对应机制而不采纳。

#### 规则摘要

- 新增 `scripts/check_report.py`：两级判定。**结构类 R1–R6 阻断（exit 1 即不得宣称阶段一完成 / 不得进入阶段二）**；**内容类 R7–R12 只警告**（`--strict-content` 可升级）。
- 具名豁免开关替代模糊例外：`--allow-scan-pages`、`--allow-no-precise-reading`、`--user-approved-open`；**用了任一开关必须在收尾清单里声明**。
- 脚本内部异常时输出 `[!!] … 不得静默通过` 并 `exit 1`（异常不得放行为通过）。
- 新增 `assets/report-template.md`：报告固定骨架，与门禁脚本的判定点一一对应。
- 新增 `references/anti-hallucination.md`：防幻觉硬规则 A–H，其中 A（先读后引）与 B（结论须与所引原文逐字对上）为最高频幻觉源。
- 新增 `state/oq-ledger.md`：持久待确认台账，登记一次长期消解，避免反复问同一件事。
- 新增首句强制声明与固定收尾字段清单；新增文档优先级仲裁表（用户当次指令 > 文献原文 > 本技能规范 > 通用学科常识）。

#### 波及文件

| 文件 | 变更摘要 |
|---|---|
| `scripts/check_report.py` | 新建：两级门禁 + RULE_HELP + 固定输出串 + 具名开关 |
| `assets/report-template.md` | 新建：固定骨架 + 机器校验对照表 |
| `references/anti-hallucination.md` | 新建：防幻觉硬规则 A–H |
| `state/oq-ledger.md` | 新建：持久待确认台账与读写协议 |
| `SKILL.md` | 加：首句声明与收尾清单、文档优先级仲裁表、防幻觉入口、读档纪律、门禁入口、豁免开关、本文件元规则 |
| `references/phase1-translate-review.md` | 加：严重级三档、生成前自检、质量回退上限、接台账、收尾跑门禁 |
| `references/pdf-evidence-extraction.md` | 加：写盘与编码规范、回退上限、先读后引落到操作层 |
| `references/phase2-export.md` | 加：`check_report.py --gate phase2` 硬前置 |
| `optimization-record.md` | 本条 |

---

### 2026-09-15：门禁脚本验收 + 三条判定收窄（避免误报）

#### 日期说明

- 本条落盘日：2026-09-15（本机公历，已用 `Get-Date -Format yyyy-MM-dd` 核对；与上一条跨自然日，故单独成条）。

#### 需求·触因

- 对 `check_report.py` 做验收时，用真实旧产物（把上一版 HTML 报告用 pandoc 转成 Markdown）回归，发现两类误报：
  1. **R8 误报**：把表格里的图号当成读数值——`| 476 | 491 | PARAMETRIC NONLINEAR MODELS | 图 19.4、图 19.5 及比较小节 |` 被判定为"含 2 处小数值未标出处"。
  2. **R10 误报**：把术语当断言——"后验推断""统计推断"这类标准名词被判定为"推断类表述未打【推论】标注"，真实产物上一次报了 8 条。
- 另有 R9 的设计缺陷：原本按"小节"判定引文出处，而小节标题（`## 二、第 476 页`）本身已带页码，导致该规则**永远通过**，等于没检查。

#### 规则摘要

- **R8 改为只数"数值单元格"**：整格仅由数字/小数点/千分位/百分号/括号/范围连接符组成、且至少含一个小数点，才算读数值。含中文的图号表格不再触发。
- **R9 改为按"引文"逐条判定**：引文所在行及其后 2 行内必须有页码或公式号；不再按小节判。
- **R10 改为只匹配"断言式措辞"**：`由此可得 / 可以推出 / 可以推断 / 由…直接得到 / 原文未说 / 并非原文` 等，**不再匹配裸词"推断/推论"**。真实产物上的误报从 8 条降到 2 条。
- **R8 不再接受"小节里有页码"作为豁免**：页码只能说明数据出自哪一页，不能说明它是原文印出的还是自己读出来的，因此出处标注必须是 `按图读数 / 矢量反算 / 原文给出 / 目测` 之一。

#### 验收结果（8 个用例，全部符合预期）

| 用例 | rc | 结果 |
|---|---|---|
| 样例·合格 | 0 | 零问题 |
| 样例·合格 + `--gate phase2` | 0 | 零问题 |
| 样例·缺页码对账 | 1 | R1 阻断（共 5 条阻断） |
| 样例·结构缺陷（6 项注入） | 1 | R2/R3/R4/R5 阻断 + R7/R8 警告 |
| 同上 + `--gate phase2` | 1 | 追加 R6 阻断 |
| 同上 + `--user-approved-open` | 1 | R6 被抑制（回到 4 条阻断）→ 豁免开关生效 |
| 样例·仅内容缺陷 | 0 | 仅 R9/R10/R11 警告，不阻断 |
| 真实旧产物（HTML→MD） | 1 | R2 阻断 + R7/R10/R12 警告 |

#### 波及文件

| 文件 | 变更摘要 |
|---|---|
| `scripts/check_report.py` | R8 改为数值单元格判定；R9 改为按引文逐条判定；R10 收窄为断言式措辞 |
| `optimization-record.md` | 本条 |

---

### 2026-09-15：去掉 WorkBuddy 专属耦合，让技能可跨 Agent 分发

#### 日期说明

- 本条落盘日：2026-09-15（本机公历，已用 `Get-Date -Format yyyy-MM-dd` 核对）。

#### 需求·触因

- 用户问"别人用的不是 WorkBuddy 怎么办"。查证后确认：**SKILL.md 是 Agent Skills 开放标准**（`SKILL.md` + `scripts/ references/ assets/`，frontmatter 只需 `name`/`description`），Claude Code / Cursor / Codex / VS Code Copilot / Gemini CLI / Windsurf / OpenCode 等都用同一格式，只是**目录名不同**。
- 但本技能里有两个"钉子"会在别的 Agent 上出问题：
  1. `present_files` 是 WorkBuddy 专属工具，其它 Agent 没有，照抄会导致"必须调用一个不存在的工具"；
  2. `references/pdf-evidence-extraction.md` 里写死了本机的 venv 绝对路径，换机器/换人必然失效。
- 另有一个正面结论：frontmatter 里的 `agent_created` 是 WorkBuddy 专有字段，**其它 Agent 会静默忽略**，不需要删。

#### 规则摘要

- **铁律 4 改写**：约束"必须落盘成文件"，不再绑定具体工具。WorkBuddy 用 `present_files`；其它 Agent 直接给出文件路径并说明已产出。
- **去掉硬编码路径**：改为"先探测 `python`/`python3`，再决定解释器"；本机路径只作为示例保留并标注"换机器不要照抄"。同时补一条：`import pymupdf` 失败时回退 `import fitz`（旧版 PyMuPDF）。
- **`SKILL.md` 新增「跨 Agent 使用」整节**：给出各 Agent 的项目级/用户级技能目录对照表；推荐跨 Agent 时用厂商中立的 **`.agents/skills/`**（Codex 默认，Cursor / VS Code / Gemini CLI 都认，一份文件多处生效）。
- **触发差异提示**：`description` 是唯一的触发依据；保守实现（如 Copilot）需要把请求说得更贴近描述原文。
- frontmatter 新增 `version: 1.1.0`（开放标准推荐的 semver 字段，便于跨 Agent 判断版本）。

#### 波及文件

| 文件 | 变更摘要 |
|---|---|
| `SKILL.md` | 铁律 4 去工具耦合；新增「跨 Agent 使用」节（目录对照表 + `.agents/skills/` 建议 + 触发差异）；frontmatter 加 `version: 1.1.0` |
| `references/pdf-evidence-extraction.md` | 删掉写死的 venv 绝对路径，改为探测式；补 pymupdf/fitz 回退与 macOS/Linux 安装命令 |
| `references/phase1-translate-review.md` | 交付一步改为"WorkBuddy 用 present_files；其它 Agent 给路径" |
| `references/phase2-export.md` | 同上 |
| `scripts/compile_pdf.py` | 成功提示里的交付话术改为跨 Agent 表述 |
| `optimization-record.md` | 本条 |

---

### 2026-09-15：HTML→MD 残留归一化（阶段二静默丢表格的坑）+ 新增预处理器

#### 日期说明

- 本条落盘日：2026-09-15（本机公历，已用 `Get-Date -Format yyyy-MM-dd` 核对；与上一条同为当日，但主题独立，故单独成条）。

#### 需求·触因

- 用户的报告 MD 是**由 HTML 报告机械转换**来的，担心"阶段二导出会出问题"。实测（pandoc 3.11）确认这不是美观问题而是**静默丢内容**：
  1. 原始 `<table>` 块在 pandoc 里是 raw block，转 LaTeX 时**整块被丢弃且不报错**——实测 3 张表全部消失，而 `.tex` 的 returncode 仍是 0；
  2. 行内 `<sub>/<sup>` 只丢标签留内容，`y<sub>i</sub>` → 字面 `yi`；
  3. `\[…\]` 被当作**行间公式**（区间写成 `\[0.94, 0.99\]` 时有 37 处，会让区间变成段落中间的 display 公式）；
  4. `<br />` 在管道表单元格内无法表达。
- 归一化目标形式用实际内容数据决定：清点后共 154 处上下标、25 种内容，其中 `y`、`θ` 没有 Unicode 下标字形，`init`、`j(i)`、`2α` 是多字符 ⇒ **Unicode 下标方案不可行**；最终选**行内 LaTeX 数学片段**（Markdown 阅读器可渲染，pandoc/LaTeX 原生安全）。

#### 规则摘要

- 新增 `scripts/normalize_md_residue.py`：token 级**等价**改写（不改内容），并带两道安全网——① **纯文本骨架全等**（剥掉全部 HTML／Markdown／LaTeX 记号后逐字比较，必须一字不差，否则拒绝落盘）；② **不得引入 `{{`／`}}`**（那是 `check_report.py` 的 R4 判定点）。
- 三个实现要点（都由真实失败换来，勿改回去）：
  1. 合并「相邻 sub+sup」的正则必须限**标签内容不含尖括号**（`[^<>]`）。用非贪婪 `.*?` 会跨标签错配，把 `ψ<sub>kl</sub> ~ N(µ<sub>l</sub>, τ<sub>l</sub><sup>2</sup>)` 拼成乱码——dry-run 的防御性检查拦下了这次事故。
  2. 上标内容以反斜杠命令开头（如 `\mathrm{init}`）时**不再套一层花括号**，否则产生 `}}` 命中 R4。
  3. 嵌套（上标里再套下标）时，下标为单字符则不加花括号，并在 `}}` 处插入 `\!` 断开。
- 归一化会改变报告的机器可读形态：**表格由 HTML 变成管道表后，R8「数值表须标出处」对这些表开始生效**。实测 §2.5 立刻报出 1 条 R8，按证据补了「【原文给出】…取自图 19.3（p.473）」后清零。因此**改完必须重跑门禁**。
- 门禁不变（**没有**为了让报告过关去放宽任何规则；`}}` 是从生成端消除的）。
- 记入 `references/phase2-export.md` 新增的 **Step 0**（前置于一切导出，PDF 与 Word 都要做），并写明已知副作用：`$_i$` 这类片段基字符在文本模式、上下标在数学模式，手写 `.tex` 时应升级为完整数学式。

#### 验收（全部实测通过）

| 项 | 结果 |
|---|---|
| 夹具 | 归一化前的真实报告备份（含 3 张 HTML 表 / 154 处上下标 / 11 处 br / 37 对方括号转义） |
| 改写账目 | colgroup 2、合并 14、嵌套 1、单独 sub 117、单独 sup 8、strong 11、br 11、表格 3、转义 74 → 与 132/23 的原始计数完全配平 |
| 骨架核对 | 36239 = 36239，**一字不差** |
| 等价性 | 技能脚本 `--apply` 到夹具副本，与线上已归一化 MD 的 15 项探针**零不一致** |
| 门禁 | `check_report.py` 两档均 `[ok] report-gate passed (0 warnings)`、exit 0 |
| pandoc 冒烟 | `-t latex` returncode 0；**19 张 longtable 全部存活**（归一化前会丢 3 张）、6 张 `\includegraphics`、`.tex` 内零残留 HTML、0 个裸 `$` |

#### 波及文件

| 文件 | 变更摘要 |
|---|---|
| `scripts/normalize_md_residue.py` | 新建：残留归一化预处理器（dry-run 默认 / `--apply` 落盘 / 自动备份 / 双安全网） |
| `references/phase2-export.md` | 新增 **Step 0**「阶段一 MD 的 HTML 残留归一化」（含四种残留的实测行为表、命令、安全网、已知副作用）；Step 5 Word 路线改为指向 Step 0 |
| `SKILL.md` | 阶段总览的「阶段二」启动条件加归一化前置；捆绑脚本表新增该脚本一行；frontmatter `version: 1.1.0` → `1.2.0` |
| `optimization-record.md` | 本条 |

---

### 2026-09-16：新增「课题组学习解析版」输出模式（第二套骨架 + 门禁 study 模式）

#### 日期说明

- 本条落盘日：2026-09-16（本机公历，已用 `Get-Date -Format yyyy-MM-dd` 核对；与上一条跨自然日，故单独成条）。

#### 需求·触因

- 用户提出：在保留现有「组会汇报版」的前提下新增一种「课题组学习解析版」——① 按**段**翻译原文；② 范围内有图表时做**精简**讲解，但**图上出现过的每一类元素都要解释清楚**；③ 对重点内容与公式做**前后文联系**的解释，让读者尽可能容易看懂。
- 现有技能只有一套逐句骨架（`#### 句 N`），而 `SKILL.md` 与阶段一 Step 1 里"逐句／**逐段**"的粒度选项一直没有对应骨架——"逐段"是个悬空选项。本次把它实体化成第二套独立骨架。
- 硬约束：不重写技能、不改任何现有结构锚点、不把学习版塞进汇报版模板当分支、不降低现有门禁强度，且**改造后既有 8 个门禁用例结果必须完全一致**。

#### 规则摘要

- 新增第二套骨架 `assets/study-template.md`，与 `assets/report-template.md` **并列且独立**：共用 `## 〇`／`## 一`／`## 二、第 X 页`／`### n.m`／`## 首轮自检与修订记录`／`## 待确认项`／`### 附 N`，只在 `2.2` 一节分叉（`#### 第 N 段` vs `#### 句 N`）。段落编号**每页内从 1 连续**。
- 新增 `references/study-mode.md`，把三条口径写成可执行判据：
  1. **「精简」的边界**：必写"这张图回答什么问题""怎么读"＋每类元素一句；不写组会预设问答／易错点／读图局限／接口／精确读数表。
  2. **「元素讲清」的判据**：图上出现过的每一类元素（坐标轴／点／线段／符号／颜色／图例）都要有一句说明它代表什么统计量；**没出现的也要写一句"本图无散点""图内无图例"**，否则读者分不清是"没有"还是"没讲"。
  3. **「前后文联系」的判据**：每个重点公式／符号回答四件事——首次定义在哪页哪式、本页为何用它、后文还用在哪、各符号含义与量纲。
- `scripts/check_report.py` 新增 `--mode {report,study}`，**默认 `report`**。学习版新增 S 系列规则：
  - **S2（阻断）**：`#### 第 N 段` 编号连续、每段有原文与译文（替代汇报版的 R2）。段落块范围取到"下一个任意级别标题"，**不能沿用 R2 的固定行窗口**——按段翻译时一段的原文／译文很长，窗口太小会把"缺译文"误报成缺失。
  - **S7（警告，弱校验）**：图表讲解覆盖"回答什么问题／怎么读"＋六类元素。
  - **S8（警告，弱校验）**：公式小节同时给出"首次定义位置"与"后续使用位置"，且必须有页码或公式号。
  - 两模式共用 R1／R3／R4／R5／R6／R8／R9／R10／R11／R12。学习版成功串输出 `[ok] study-gate passed`，汇报版保持 `[ok] report-gate passed` 不变。
- 两条由实测换来的收窄（勿改回去）：
  1. **S7 匹配前先剥掉 `**` 加粗记号**——`- **点**：…` 这种写法会让"点"被两个星号挡住，误报"没讲点"。
  2. **S8 只认承载"前后文联系"职责的公式小节标题**（含"公式／前后文／符号说明"），并**排除 `### 附 N`**——否则附录里"本页用到的前文接口：式 (19.4)"会被当成前后文小节而误报。
- `description` 加入学习版触发词（课题组学习／学习解析版／按段翻译／便于看懂版），并写明**默认仍是组会汇报版**；`SKILL.md` 新增「两种输出模式」一节（判定顺序 + "两版都要就产两个文件"）。

#### 验收（全部实测）

| 项 | 结果 |
|---|---|
| 既有 8 个门禁用例 | 改造后与改造前 `diff` **完全一致**（`acceptance_before.txt` vs `acceptance.txt`，逐字节 IDENTICAL） |
| 学习版·合格 | `rc=0`，0 阻断 0 警告 |
| 学习版·合格 + `--gate phase2` | `rc=0`，0 阻断 0 警告 |
| 学习版·段编号跳号（第 1 段 → 第 3 段） | `rc=1`，S2 阻断 |
| 学习版·图表元素缺失（漏符号／颜色／图例） | `rc=0`，仅 S7 警告，不阻断 |
| 负对照：汇报版样例按 `--mode study` 查 | `rc=1`，S2 阻断（证明模式分流确实生效） |
| 真实文献成品（BDA3 书 p.476–477） | `[ok] study-gate passed (0 warnings)`，`rc=0`；`--gate phase2` 同样通过 |

#### 波及文件

| 文件 | 变更摘要 |
|---|---|
| `assets/study-template.md` | 新建：学习版固定骨架 + `--mode study` 的机器校验对照表 |
| `references/study-mode.md` | 新建：学习版执行细则（三条操作定义、按段写法、图表八条、前后文四问、门禁命令） |
| `scripts/check_report.py` | 新增 `--mode`（默认 report）与 S2／S7／S8；新增 `PARA_PAT`、`STUDY_ELEMENT_KEYWORDS`、`STUDY_FIG_MUST`、`FORMULA_SEC_PAT`、`FIRST_DEF_PAT`、`LATER_USE_PAT`、`_para_extent()`；`RULE_HELP` 补 S2／S7／S8；report 分支的调用顺序与输出串一字未动 |
| `SKILL.md` | `description` 加学习版触发词并写明默认为汇报版；新增「两种输出模式」一节；阶段总览、读档纪律、执行入口、捆绑脚本表、命令示例同步补学习版；`version: 1.2.0` → `1.3.0` |
| `references/phase1-translate-review.md` | 产出树注明 `_学习版.md`；Step 1 第 3 项由"汇报粒度：逐句／逐段"改为"输出模式"（逐段归学习版）；Step 3 补学习版骨架指引；Step 6 补 `--mode study` 门禁命令 |
| `references/phase2-export.md` | 前置条件补学习版的 `--gate phase2 --mode study` 命令 |
| `optimization-record.md` | 本条 |

---

### 2026-09-16：纠正本机执行环境记载（Bash 可用；PowerShell 不启动外部进程）

#### 日期说明

- 本条落盘日：2026-09-16（本机公历，已用 `Get-Date -Format yyyy-MM-dd` 核对；与上一条同日、主题独立，故单独成条）。

#### 需求·触因

- 本轮做学习版改造时，按技能原记载"Bash 在本机不可用（缺 coreutils），且 PowerShell 的 stdout 不会被工具捕获"操作，把命令全部交给 PowerShell 工具并重定向到文件——**结果命令返回退出码 0，但目标文件一个都没生成**，一度被误判成"脚本已经跑过、基线已经固化"。
- 改用 Bash 通道后立刻正常：`ls / cp / rm / mkdir / diff / grep / wc / head / tail` 等命令全部可用，Python 的 stdout（含中文）能被工具直接捕获，无需任何重定向。
- 结论：原记载把两个环境事实写反了，而且写反的代价是**静默失败**——命令报成功、实际什么都没做，是最危险的一类错误。

#### 规则摘要

- **执行外部命令（python / pandoc / xelatex）一律走 Bash 通道**，stdout 直接读，不必再"先重定向到文件再 Read"。
- **禁止把外部命令交给 PowerShell 工具**：实测它返回 `exit code 0` 却根本没有启动进程，命令无任何产物，最容易被误判成执行成功。PowerShell 只用于 `Get-Date`、`Compress-Archive` 这类 cmdlet。
- 原有两条仍然成立的 PowerShell 注意事项予以保留：`*>` 重定向出来是 UTF-16（读前先转码）；不要用 PowerShell 管道／`Set-Content`／`Out-File` 直写含中文的文件。

#### 波及文件

| 文件 | 变更摘要 |
|---|---|
| `SKILL.md` | 「已知环境」末条由"Bash 不可用 + PowerShell stdout 不被捕获"改为"外部命令走 Bash 通道；禁止交给 PowerShell 工具（实测不启动进程）" |
| `references/pdf-evidence-extraction.md` | §0 运行环境的同一条记载同步改写，命令示例由 PowerShell 改为 Bash，并保留 UTF-16 转码注意事项 |
| `optimization-record.md` | 本条 |

---

### 2026-09-16：学习版首次导出 PDF 实测——两个 LaTeX 坑 + 打包排除工作区数据

#### 日期说明

- 本条落盘日：2026-09-16（本机公历，已用 `Get-Date -Format yyyy-MM-dd` 核对；与前两条同日、主题独立，故单独成条）。

#### 需求·触因

- 用户要求把 BDA3 书 p.476–477 的学习版也导出成 PDF（与已有汇报版 PDF 同规格）。这是学习版第一次真正走完阶段二，暴露两个新问题：
  1. **表格比版心宽 21.9pt**：`\setlength{\parindent}{2em}` 之下，`tabularx` 被当作段落首行缩进了一个 2em，整表按 `\linewidth` 排完再左移 2em，右边界于是越出版心 21.9pt（= 2em @ 11pt）。**换列格式、把 `p{}` 调窄都无效**——错的是缩进，不是列宽。日志表现是 `Overfull \hbox (21.9pt too wide)`，且两张表报出的宽度完全相同，这是识别它的特征。
  2. **附录图的裁剪框按图本身定、没按版心定**：`fig19_1/19_2/19_7` 的裁剪框左右边界落在书页版心之内，把书页上印刷的图注文字左右各切掉一两个字（渲染成 ``igure 19.7 Concentration…`` 这类断头文字）；`fig19_7` 的框还把页眉一起框了进来。本样本书页 504×720pt，版心实测 x∈[54.1, 448.7]、页眉区 y<56。

#### 规则摘要

- **`\parindent` 非零时，所有 `tabularx` / `tabular` 前必须加 `\noindent`**（或局部把 `\parindent` 置零）。判定特征：`Overfull \hbox` 的超出量恰好等于 `\parindent`。
- **交付前自检清单新增一条**：编译日志里不得出现 `Overfull \hbox`。
- **从书页裁图时按书页版心定框，不按图本身的视觉边界定框**：先用 `get_text("words")` 量出该页词坐标的 min/max x 作为左右边界，y 范围取"图 + 其印刷图注"的完整区间，并避开页眉/页脚区。
- **打包分发前排除 `.workbuddy-ai/`**：那是工作区记忆与缓存，不是技能内容；直接对整个技能目录打包会把它一起发出去。做法是先复制到临时目录（跳过 `.workbuddy-ai/`、`__pycache__`、`*.pyc`）再对副本打包。

#### 验收（全部实测）

| 项 | 结果 |
|---|---|
| 学习版 PDF 编译 | `latexmk -xelatex` 成功，12 页，`错误清单 : 无致命错误`，且**零 `Overfull \hbox`** |
| 版式核对 | 两张表右边界与版心对齐（表格横线 x 65.2→530.1，与词坐标一致）；附录图注左右完整、无页眉混入 |
| 学习版 MD 门禁 | 补入附录图后重跑，`--gate phase2 --mode study` 仍 `[ok] study-gate passed (0 warnings)` |
| 打包 | 对排除 `.workbuddy-ai/` 的临时副本打包，ZIP 内 15 个文件，不再含记忆文件；`package_skill.py` 输出 `Skill is valid!` |

#### 波及文件

| 文件 | 变更摘要 |
|---|---|
| `references/latex-cookbook.md` | §4 CJK 坑表新增"表格比版心宽出一个缩进量"一条（含 21.9pt = 2em 的识别特征与 `\noindent` 对策）；§5 交付前自检清单新增"日志里不得有 Overfull hbox" |
| `SKILL.md` | 元规则一节新增"打包分发时先排除工作区数据 `.workbuddy-ai/`" |
| `optimization-record.md` | 本条 |

---

### 2026-09-16：新增铁律 5「输出目录先行确认」

#### 日期说明

- 本条落盘日：2026-09-16（本机公历，已用 `Get-Date -Format yyyy-MM-dd` 核对；与前三条同日、主题独立，故单独成条）。

#### 需求·触因

- 用户要求增加一条强制门禁：**必须在生成文件前确认用户指定放在哪个文件夹，且必须放在指定文件夹**。
- 原有四条铁律里，铁律 4 只约束"必须落盘成文件"，没约束"落到哪里"；阶段一 Step 1 的必问项也只有文献路径、页码范围、粒度、听众——**输出目录从头到尾没有人问过**。实际执行时就会随手写进当前工作目录，用户事后还得自己搬。

#### 规则摘要

- **新增铁律 5「输出目录先行确认」**（四条铁律变五条）：
  - **写任何文件之前**必须先向用户确认输出目录；
  - 用户指定了就**只能写进那个目录**，不许改写别处、不许顺手多存一份；
  - 用户没指定、或说"随便／你定"时，**报出建议目录并停下等一句确认**，不许默认写当前工作目录、不许拿上次会话的目录顶替；
  - 覆盖**所有交付给用户的文件**（含 `figures/`、`evidence/`）；阶段二的 `.tex`、`build/`、PDF 同理——可以在阶段一目录下开子目录，但必须用户认可。
- 阶段一 **Step 1 的必问项把"输出目录"提到第 1 条**，并写明"四项齐了才开工"：文献路径、页码范围、输出模式、输出目录。
- 收尾固定字段清单新增 **「输出目录」** 一行（写明"写入前已确认"）。
- 开场声明之后的第一件事就是确认输出目录。

#### 验收

| 项 | 结果 |
|---|---|
| 既有 8 个门禁用例 | 与改造前逐字节一致（本次只改文档，未动判定逻辑） |
| 学习版 6 个用例 | 全部维持原结果 |
| 打包 | 对排除 `.workbuddy-ai/` 的临时副本打包，输出 `Skill is valid!` |
| 改前备份 | 已备份 v1.3.0（`_work/backup/group-meeting-lit-report_v1.3.0_2026-09-16.zip`，testzip 通过、成员数与源文件数一致） |

#### 波及文件

| 文件 | 变更摘要 |
|---|---|
| `SKILL.md` | 四条铁律 → **五条铁律**，新增铁律 5「输出目录先行确认」；开场必做补"声明后第一件事是确认输出目录"；收尾字段清单新增「输出目录」行；`version: 1.3.0` → `1.4.0` |
| `references/phase1-translate-review.md` | 产出树根目录由 `<工作目录>/` 改为 `<用户指定目录>/`；Step 1 必问项新增第 1 条「输出目录」，并写明"四项齐了才开工" |
| `references/phase2-export.md` | 前置条件新增"阶段二输出目录已确认"（含子目录需用户认可） |
| `references/study-mode.md` | "四条铁律"改为"五条铁律"，并补一句学习版同样受输出目录约束 |
| `assets/study-template.md` | 用法说明改为"先按铁律 5 确认输出目录，再复制到 `<用户指定目录>/…`" |
| `optimization-record.md` | 本条 |

---

### 2026-09-16：交付物与过程凭据分离 + 输出目录闸门 + 图表解释补两条

#### 日期说明

- 本条落盘日：2026-09-16（本机公历，已用 `Get-Date -Format yyyy-MM-dd` 核对；与本日前面几条主题独立，故单独成条）。

#### 需求·触因

- 用户一次提了四件事：
  1. **加输出目录闸门**：铁律 5 当时只有文档约束、没有机器校验，要求补上脚本层核对；
  2. **交付物里不要「首轮自检与修订记录」与「待确认项」**：那是过程凭据，读者不需要，改在阶段任务完成时说明；
  3. **图表解释不够**：只描述了画面，没交代"作者为什么在这里放这张图"与"这张图还能说明什么"；
  4. **PDF 列表排版不好看**：加粗标签与正文同行，但续行被固定缩进到 3.6cm，与首行左边缘对不齐（实测标签 x=65.2pt、续行 x=167.2pt）。

#### 规则摘要

- **交付物与过程凭据分离**：`## 首轮自检与修订记录` 与 `## 待确认项` 移出交付物，写进 `<用户指定目录>/_review/<简称>_p<起>-<止>_审查记录.md`（新增骨架 `assets/review-template.md`）。门禁新增 `--review <文件>`，R5／R6／R12 改从"交付物 + 审查记录"合并后的文本判定；**不给 `--review` 时行为与以前完全一致**。这两项只在收尾固定字段清单里汇报。
- **输出目录闸门 R13**：门禁新增 `--outdir "<用户确认的目录>"`，核对报告实际所在目录是否落在该目录内，不在就**阻断**。**不提供 `--outdir` 时不做任何判定**，既有用例结果不变。
- **图表解释补两条**（两模式同步）：
  - 汇报版必讲清单 **10 条 → 12 条**，新增第 2 条「为什么要有这张图」与第 11 条「这张图还能说明什么」；
  - 学习版图表条目 **8 条 → 10 条**，同样补这两条（S7 机器检查同步扩到 10 条）；
  - 门禁新增 `--figures-full`，R7 在四要素之外再查这两项；**默认不开**，因为开了会让既有 8 个用例的警告数变化（既有样例是按旧清单写的）。
  - 两条新内容天生"超出原文"，防幻觉规则 F 补两句：第 11 条每写一句都要自问"这是图上真能看出来的，还是我希望它说明的"；第 2 条不许替作者编动机，推不出来就标 `<待确认:作者为何在此处给出该图>`。
- **PDF 列表排版**：LaTeX 侧弃用 `description` + 悬挂缩进，改为 `\KeyItem{标签}{正文}`——标签加粗与正文同行，**续行回到左边距**，与标签左对齐（改后续行 x=65.2pt，与标签一致）。

#### 验收（全部实测）

| 项 | 结果 |
|---|---|
| 既有 8 个门禁用例 | 与改造前**逐字节一致** |
| 学习版 6 个骨架用例 | 全部维持原结果 |
| 学习版·漏两条新增项 | `rc=0`，S7 警告 |
| 学习版·交付物无两节 + `--review` | `rc=0`，0 警告 |
| 学习版·交付物无两节、不给 `--review` | `rc=1`，R5 + R12 阻断 |
| `--outdir` 一致 / 不一致 | `rc=0` / `rc=1`，R13 阻断 |
| 汇报版 `--figures-full` | `rc=0`，R7 警告（旧清单缺两条） |
| 学习版成品（BDA3 书 p.476–477） | 带 `--outdir --review --gate phase2 --mode study` 通过，0 警告 |
| 学习版 PDF | `latexmk -xelatex` 成功，11 页，零致命错误、零 `Overfull \hbox`；列表续行与标签左对齐 |

#### 波及文件

| 文件 | 变更摘要 |
|---|---|
| `scripts/check_report.py` | 新增 `--outdir`（R13 阻断）／`--review`（R5/R6/R12 从审查记录合并判定）／`--figures-full`（R7 完整清单）；新增 `FIG_SECTION_KEYWORDS_FULL`，`STUDY_FIG_MUST` 扩到四项（S7 共十条）；`RULE_HELP` 补 R13 并更新 R7／S7；补 `import os` |
| `assets/review-template.md` | 新建：审查记录骨架（自检与修订记录 + 待确认项），说明为什么移出交付物、门禁怎么指过来 |
| `assets/report-template.md` | 骨架去掉两节；2.3／3.3 图表详解补「为什么要有这张图」「这张图还能说明什么」；用法说明与机器校验对照表同步（R5/R6/R7/R12 行改写，新增 R13 行） |
| `assets/study-template.md` | 同上（图表条目补两条、去掉两节、对照表补 S7 十条与 R13） |
| `references/phase1-translate-review.md` | 产出树补审查记录文件并注明交付物不含两节；必讲清单 10 → 12 条并加"只描述画面不算讲图"的判据；Step 5／6 与门禁命令同步 |
| `references/study-mode.md` | 图表八条 → 十条并逐条说明新增两项；必写项与门禁命令同步 |
| `references/phase2-export.md` | 阶段二前置条件与门禁命令补 `--outdir --review --figures-full` |
| `references/anti-hallucination.md` | 规则 F 补两条：新增的两项天生超出原文，逐句自问"图上真能看出来还是我希望它说明"；不许替作者编动机 |
| `SKILL.md` | 收尾清单说明两项过程凭据只在此汇报；阶段总览的交付物与启动条件（加输出目录）更新；执行入口补 `review-template.md`；捆绑脚本表与命令示例补新参数；`version: 1.4.0` → `1.5.0` |
| `optimization-record.md` | 本条 |

